# Anthony & Stephanie Travel Planner

## Netlify Settings

Build command:

```bash
npm run build
```

Publish directory:

```text
dist
```

Base directory: leave blank.

## GitHub Upload Instructions

Upload the contents of this folder to GitHub so that `package.json`, `index.html`, `netlify.toml`, and the `src` folder are visible at the top level of the repository.
